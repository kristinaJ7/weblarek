# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

Документация проекта

Раздел «Данные»

В нём описаны интерфейсы данных, которые были разработаны при разборе проекта
В приложении используются следующие интерфейсы для описания структур данных:

1. IProduct — интерфейс товара (хранит массив всех товаров;)
   Описывает структуру данных о товаре, доступном для покупки.

Поля:

- id: string — уникальный идентификатор товара;

- description: string — описание товара;

- image: string — URL изображения товара;

- title: string — название товара;

- category: string — категория товара;

- price: number \| null — цена товара (может быть null, если цена не указана).

2. IBuyer — интерфейс покупателя
   Описывает данные покупателя, необходимые для оформления заказа.

Поля:

- payment: TPayment — способ оплаты ("card" или "cash");

- email: string — электронная почта покупателя;

- phone: string — номер телефона покупателя;

- address: string — адрес доставки.

3. TPayment — тип способа оплаты
   Перечисление допустимых значений: "card" (карта) или "cash" (наличные).

«Модели данных» — Описаны все классы, которыми реализована работа с данными.

1. Класс Products (каталог товаров)

Назначение и зона ответственности класса:
Управляет коллекцией товаров, доступных в магазине. Отвечает за:

- хранение полного списка доступных товаров;

- поиск товаров по уникальному идентификатору;

- управление текущим выбранным товаром (для отображения детальной информации);

- предоставление доступа к данным каталога для других компонентов системы.

Поля:

- \_productsList: IProduct[] — список всех товаров ([] по умолчанию);

- \_selectedProduct: IProduct | null — выбранный товар (null по умолчанию).

Методы:

- setItems(items: IProduct[]): void

Заменяет список товаров на переданный массив.

- getProducts(): IProduct[]

Возвращает полный список товаров.

- getProductById(id: string): IProduct | null

Ищем товар по id; возвращает товар или null.

- setSelectedProduct(product: IProduct): void

Сохраняет переданный товар как выбранный.

- getSelectedProduct(): IProduct | null

Возвращает выбранный товар или null.

Интерфейс IBuyer (покупатель)
Назначение: определяет структуру данных покупателя для оформления заказа.

Поля:

- payment: TPayment — способ оплаты ("card" или "cash");

- email: string — email покупателя (обязательное поле);

- phone: string — телефон покупателя (обязательное поле);

- address: string — адрес доставки (обязательное поле).

TPayment — тип способа оплаты
Перечисление допустимых значений:

- type TPayment = "card" | "cash";

Подключение работы с сервером

1. Раздел «Слой коммуникации» в документации

Реализован слой коммуникации приложения — класс, обеспечивающий взаимодействие с сервером «Веб‑ларёк» через API. Класс получает:

- каталог товаров (GET /product/);
- данные заказа (POST /order/).

Ключевой класс: ApiService

- Представитель коммуникационного слоя — он отвечает за получение данных с сервера и отправку данных на сервер.
- При работе использована функциональность класса API из стартового набора.
- Класс используе композицию, чтобы выполнить запрос на сервер с помощью метода get класса Api и получает с сервера объект с массивом товаров.
- Использован метод post класса Api, чтобы класс мог отправлять на сервер данные о покупателе и выбранных товарах.

В классе два метода:

- Один делает get запрос на эндпоинт /product/ и возвращает массив товаров.
- Второй делает post запрос на эндпоинт /order/ и передаёт в него данные, полученные в параметрах метода.









































# Шаг 3. Документация классов слоя View (представление классы, которые будут отвечать за отображение интерфейса.)

# Общая архитектура

- Слой представления реализует принцип «один класс — один блок разметки». Все компоненты строго разделены по функциональному назначению. Для однотипных элементов (карточки товаров, формы) применено наследование с вынесением общей логики в родительские классы.

# Классы представления

(каждый класс представления отвечает за свой блок разметки)

# Header

```
класс для представления компонента шапки сайта с кнопкой корзины и счётчиком товаров
Header
```

Содержимое:

```
кнопка корзины (.header__basket);
счётчик товаров (.header__basket-counter).
События:

"basket:open" — эмиттится при клике на кнопку корзины.
Метод:

set counter(value: number) — обновляет значение счётчика на экране.
```

# Карточки

```
- класс для отображение карточки товара в каталоге.
  CardCatalog

  Входные данные:

product: объект IProduct (обязателен);
container: корневой элемент карточки (обязателен);
actions: обработчики событий (опционально, включает onClick).
Ключевые методы (сеттеры):

category(value) — устанавливает категорию и добавляет соответствующий CSS‑класс.
title(value) — устанавливает название товара.
image(value) — задаёт URL изображения (с обработкой ошибок загрузки).
price(value) — отображает цену или «Бесплатно» (если null).
```

```
- отображение детальной карточки товара (в модальном окне), управление действиями «Купить»/«Удалить из корзины».
  CardPreview

  Входные данные:

container: корневой элемент карточки;
actions: обработчики событий (ICardPreviewActions с методами onClickBuy и onClickRemove).
Ключевые методы:

fillData(product) — заполняет карточку данными товара, устанавливает начальное состояние кнопки.
setInCart(isInCart) — переключает состояние кнопки («Купить» ↔ «Удалить из корзины»).
getContainer() — возвращает корневой элемент.
Сеттеры (для заполнения данных):

category, title, text — текст элементов;
image — URL изображения (с обработкой ошибок);
price — цена (или «Бесплатно», если null).
```

# Форма

```
- два класса для форм имеют одного общего родителя ( в который выносится общий для всех форм функционал)
  OrderForm

Наследуемые классы:

- класс для формы контактов
  ContactsForm

- класс для формы адреса
  FormAddress
```

- OrderForm
  Обязательные абстрактные методы (должны быть реализованы в дочерних классах):

getFormData(): T — возвращает данные формы в виде объекта.
validate(data: T): string[] — проверяет данные, возвращает массив строк‑ошибок.
Публичные методы:

```
setErrors(message) — отображает сообщение об ошибке.
setSubmitEnabled(enabled) — включает/отключает кнопку отправки.
clearErrors() — очищает сообщения об ошибках.
getContainer() — возвращает корневой элемент формы.
```

- ContactsForm
  форма для ввода контактных данных (email и телефон) с валидацией и отправкой.
  Наследует: OrderForm<IContactsData> (общий функционал форм).
  Ключевые методы:

```
getFormData() — собирает значения полей в объект IContactsData.
validate(data) — проверяет корректность email и телефона, возвращает массив ошибок.
forceValidate() — принудительно запускает валидацию.
initEventListeners() — подписывается на изменения полей, эмиттит события при вводе.
```

- FormAddress
  Назначение: форма для ввода адреса доставки и выбора способа оплаты.

Наследует: OrderForm<IAddressData> (общий функционал форм).
Ключевые методы:

```
getFormData() — собирает данные формы (выбранный метод оплаты и адрес).
validate(data) — проверяет:
выбран ли способ оплаты;
заполнен ли адрес.
setPaymentSelected(method) — выделяет выбранную кнопку оплаты, запускает валидацию.
validateAndUpdate() — выполняет валидацию и обновляет состояние формы.
```

# Модальное окно

модальное окно не имеет дочерних классов, от него не могут наследоваться другие классы. Вся разметка, которая отображается в модальных окнах, — это самостоятельные компоненты. Они могут быть выведены в любой блок сайта, в том числе и в модальном окне.

```
   ModalWindow
```

Параметры конструктора:

```
events — брокер событий;
closeSelector (опц.) — селектор кнопки закрытия (по умолчанию .modal__close);
contentSelector (опц.) — селектор контейнера контента (по умолчанию .modal__content).
Основные методы:

open(content: HTMLElement) — открывает окно с указанным контентом.
close() — закрывает окно, очищает контент.
show(options: ModalOptions) — настраивает и открывает окно (с заголовком, настройками закрывания и др.).

```

# Корзина

отображение корзины с товарами, общей суммой и кнопкой оформления заказа.

```
 для представления корзины создан класс
  Basket
Входные данные (IBasketData):

items: массив товаров (IProduct[]);
total: общая сумма (число).
Ключевые методы:

render(data) — обновляет корзину (сумму и список товаров).
set items(value) — заполняет список товаров (или показывает «Корзина пуста»).
set total(value) — устанавливает общую сумму в «синапсах».
getContainer() — возвращает корневой элемент корзины.
```

```
-
 класс созданный для представления товара в корзине при добавлении(отображение отдельной карточки товара в корзине)
  BasketProduct

Входные данные:

IProduct — данные о товаре;
actions.onClickDelete — обработчик удаления товара (передаётся извне).
Ключевые методы (сеттеры):

index(value: number) — устанавливает порядковый номер в корзине.
title(value: string) — устанавливает название товара.
price(value: number) — устанавливает цену (в «синапсах»).

```

# Блок Success

```
- показывает экран успеха после оформления заказа
SuccessOrder

Главное событие: "success:close" (при нажатии кнопки закрытия).
 Ключевые методы:

- render({ total }) — обновить сумму;
- getContainer() — получить элемент для вставки в DOM.
```
